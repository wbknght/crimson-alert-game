export const GameConfig = {
    TILE_WIDTH: 64,
    TILE_HEIGHT: 32,
    // Default offset to center the map roughly
    DEFAULT_OFFSET_X: 0, // Will be set dynamically based on screen width usually, but good to have a base
    DEFAULT_OFFSET_Y: 100,
};
